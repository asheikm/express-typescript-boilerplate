version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14
    steps:
      - checkout
      - run: yarn add bcrypt-nodejs && yarn remove bcrypt
      - run: yarn install
      - run: yarn test

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
